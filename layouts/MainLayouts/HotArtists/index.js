import Session from "../../../components/Session";
import styles from "./styles.module.scss";

const HotArtists = () => (
  <div className={styles["artists-wrapper"]}>
    <Session sessionTitle="Hot Artists" />
  </div>
);

export default HotArtists;
