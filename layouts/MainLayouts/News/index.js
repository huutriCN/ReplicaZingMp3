import Session from "../../../components/Session";
import styles from "./styles.module.scss";

const News = () => (
  <div className={styles["news-wrapper"]}>
    <Session sessionTitle="News" />
  </div>
);

export default News;
