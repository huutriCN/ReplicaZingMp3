import styles from "./styles.module.scss";

const Partners = () => (
  <div className={styles["partner-wrapper"]}>
    <div className={styles["partner-wrapper-inner"]}>Partners</div>
  </div>
);

export default Partners;
