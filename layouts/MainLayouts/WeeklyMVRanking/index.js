import Session from "../../../components/Session";
import styles from "./styles.module.scss";

const WeeklyMVRanking = () => (
  <div className={styles["ranking-wrapper"]}>
    <Session sessionTitle="Weely MV Ranking" />
  </div>
);

export default WeeklyMVRanking;
