import styles from "./ChartRanking.module.scss";

const ChartRanking = ({ className }) => (
  <div className={`${styles.chartRanking} ${className}`}>Chart Ranking</div>
);

export default ChartRanking;
